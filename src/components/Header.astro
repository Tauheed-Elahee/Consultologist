---
---

<header>
	<fluent-toolbar>
		<fluent-anchor href="/" slot="start">
			<span>Consultologist</span>
		</fluent-anchor>

		<div slot="end" style="display: flex; gap: 1rem; align-items: center;">
			<fluent-anchor href="/#home" data-scroll-target="#home">Home</fluent-anchor>
			<fluent-anchor href="/#how-it-works" data-scroll-target="#how-it-works">How it works</fluent-anchor>
			<fluent-anchor href="/#features" data-scroll-target="#features">Features</fluent-anchor>
			<fluent-anchor href="/#pricing" data-scroll-target="#pricing">Pricing</fluent-anchor>
			<fluent-anchor href="/#blog" data-scroll-target="#blog">Blog</fluent-anchor>
			<fluent-anchor href="https://app.consultologist.ai">Sign in</fluent-anchor>
			<fluent-button appearance="accent" href="/demo">Try it free</fluent-button>
		</div>

		<fluent-button id="nav-toggle" slot="end" class="mobile-toggle">☰</fluent-button>
	</fluent-toolbar>

	<div id="mobile-menu" class="mobile-menu">
		<fluent-anchor href="/#home" data-scroll-target="#home">Home</fluent-anchor>
		<fluent-anchor href="/#how-it-works" data-scroll-target="#how-it-works">How it works</fluent-anchor>
		<fluent-anchor href="/#features" data-scroll-target="#features">Features</fluent-anchor>
		<fluent-anchor href="/#pricing" data-scroll-target="#pricing">Pricing</fluent-anchor>
		<fluent-anchor href="/#blog" data-scroll-target="#blog">Blog</fluent-anchor>
		<fluent-anchor href="https://app.consultologist.ai">Sign in</fluent-anchor>
		<fluent-button appearance="accent" href="/demo">Try it free</fluent-button>
	</div>
</header>

<style>
	header {
		position: fixed;
		top: 0;
		left: 0;
		right: 0;
		z-index: 1000;
		background: var(--colorNeutralBackground1);
		box-shadow: var(--shadow4);
	}

	.mobile-toggle {
		display: none;
	}

	.mobile-menu {
		display: none;
		flex-direction: column;
		padding: var(--spacingVerticalM);
		gap: var(--spacingVerticalS);
		background: var(--colorNeutralBackground1);
	}

	.mobile-menu.active {
		display: flex;
	}

	@media (max-width: 768px) {
		fluent-toolbar div[slot="end"]:not(.mobile-toggle) {
			display: none;
		}

		.mobile-toggle {
			display: block;
		}
	}
</style>

<script>
	const navToggle = document.getElementById("nav-toggle");
	const mobileMenu = document.getElementById("mobile-menu");

	if (navToggle && mobileMenu) {
		navToggle.addEventListener("click", () => {
			mobileMenu.classList.toggle("active");
		});

		const mobileLinks = mobileMenu.querySelectorAll("fluent-anchor, fluent-button");
		mobileLinks.forEach((link) => {
			link.addEventListener("click", () => {
				mobileMenu.classList.remove("active");
			});
		});
	}

	document
		.querySelectorAll<HTMLElement>("fluent-anchor[data-scroll-target]")
		.forEach((link) => {
			const targetSelector = link.getAttribute("data-scroll-target");
			if (!targetSelector) {
				return;
			}

			const href = link.getAttribute("href");
			if (!href) return;

			const linkUrl = new URL(href, window.location.origin);
			link.addEventListener("click", (event) => {
				if (linkUrl.pathname !== window.location.pathname) {
					return;
				}

				event.preventDefault();
				const target = document.querySelector(targetSelector);
				if (target) {
					target.scrollIntoView({
						behavior: "smooth",
						block: "start",
					});
				}
			});
		});
</script>

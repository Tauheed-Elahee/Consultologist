---
---

<section id="how-it-works">
	<div>
		<h2>Easy as 1, 2, 3</h2>
		<p>Create your custom consult notes instantly.</p>
	</div>

	<div class="steps-grid">
		<fluent-card>
			<fluent-badge>1</fluent-badge>
			<h3>Send referral package</h3>
			<p>
				Fax the referral package to your unique Consultologist number and have your MOA send out a custom patient questionnaire.
			</p>
		</fluent-card>

		<fluent-card>
			<fluent-badge>2</fluent-badge>
			<h3>Our AI drafts the note</h3>
			<p>
				Consultologist creates your consult note by transforming the data from the referral package and patient questionnaire.
			</p>
		</fluent-card>

		<fluent-card>
			<fluent-badge>3</fluent-badge>
			<h3>Copy to EMR</h3>
			<p>
				Copy the drafted consult note directly into your EMR and proceed with your typical clinical encounter.
			</p>
		</fluent-card>
	</div>

	<fluent-button appearance="accent" href="/demo">
		Try it free - save 4+ hours every week
	</fluent-button>
</section>

<style>
	section {
		padding: var(--spacingVerticalXXXL) var(--spacingHorizontalL);
		background: var(--colorNeutralBackground2);
		text-align: center;
	}

	h2 {
		color: var(--colorNeutralForeground1);
		margin-bottom: var(--spacingVerticalM);
	}

	section > p {
		color: var(--colorNeutralForeground2);
		margin-bottom: var(--spacingVerticalXXL);
	}

	.steps-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
		gap: var(--spacingHorizontalXL);
		margin-bottom: var(--spacingVerticalXXL);
	}

	fluent-card {
		padding: var(--spacingVerticalXL) var(--spacingHorizontalL);
		text-align: center;
	}

	fluent-badge {
		margin-bottom: var(--spacingVerticalL);
	}

	h3 {
		color: var(--colorNeutralForeground1);
		margin-bottom: var(--spacingVerticalM);
	}

	fluent-card p {
		color: var(--colorNeutralForeground2);
	}
</style>

---
// How it Works section matching the new design
---

<section id="pricing" class="pricing">
	<div class="container">
		<div class="section-header">
			<h2 class="section-title">Contract</h2>
			<p class="section-subtitle">
				Inaugurate your department of Consultology
			</p>
		</div>

		<div class="pricing-grid">
			<article class="pricing-card">
				<p class="plan-name">Attending</p>
				<p class="plan-description">
					Perfect for solo clinicians looking to automate
					documentation.
				</p>
				<p class="plan-price">
					<span class="price-amount">$250</span>
					<span class="price-period">/month</span>
				</p>
				<ul class="plan-features">
					<li>Up to 75 consult notes per month</li>
					<li>Secure Consultologist fax number</li>
					<li>Access to baseline templates</li>
					<li>Email support</li>
				</ul>
				<a class="plan-button" href="/demo">Start free trial</a>
			</article>

			<article class="pricing-card">
				<p class="plan-name">Resident</p>
				<p class="plan-description">
					Designed for growing teams who need deeper customization.
				</p>
				<p class="plan-price">
					<span class="price-amount">$125</span>
					<span class="price-period">/month</span>
				</p>
				<ul class="plan-features">
					<li>Unlimited consult notes</li>
					<li>Advanced workflow automation</li>
					<li>Customizable templates & branding</li>
					<li>Priority in-app support</li>
				</ul>
				<a class="plan-button" href="/demo">Book a demo</a>
			</article>

			<article class="pricing-card">
				<p class="plan-name">Student</p>
				<p class="plan-description">
					For organizations that require scale, compliance, and
					support.
				</p>
				<p class="plan-price">
					<span class="price-amount">$75</span>
					<span class="price-period">/month</span>
				</p>
				<ul class="plan-features">
					<li>Dedicated success manager</li>
					<li>Enterprise-grade security controls</li>
					<li>Deep EHR integrations</li>
					<li>24/7 support & onboarding</li>
				</ul>
				<a class="plan-button" href="/contact">Talk to sales</a>
			</article>
		</div>

		<div class="cta-section">
			<a href="/demo" class="cta-button"
				>Try it free - save 4+ hours every week</a
			>
		</div>
	</div>
</section>

<style>
	.pricing {
		padding: 6rem 2rem;
		background: white;
	}

	.container {
		max-width: 1200px;
		margin: 0 auto;
	}

	.section-header {
		text-align: center;
		margin-bottom: 4rem;
	}

	.section-title {
		font-size: 3rem;
		font-weight: 700;
		color: #1f2937;
		margin-bottom: 1rem;
	}

	.section-subtitle {
		font-size: 1.25rem;
		color: #6b7280;
		max-width: 600px;
		margin: 0 auto;
		line-height: 1.6;
	}

	.pricing-grid {
		display: grid;
		grid-template-columns: repeat(3, minmax(0, 1fr));
		gap: 2.5rem;
		margin-bottom: 4rem;
	}

	.pricing-card {
		background: rgba(255, 255, 255, 0.95);
		border: 1px solid #e5e7eb;
		border-radius: 24px;
		box-shadow: 0 20px 45px rgba(15, 23, 42, 0.08);
		padding: 2.5rem 2rem;
		display: flex;
		flex-direction: column;
		gap: 1.5rem;
		min-height: 100%;
		position: relative;
		transition:
			transform 0.3s ease,
			box-shadow 0.3s ease;
	}

	.pricing-card:hover {
		transform: translateY(-8px);
		box-shadow: 0 30px 50px rgba(15, 23, 42, 0.15);
	}

	.pricing-card.featured {
		background: linear-gradient(145deg, #0f766e, #0d9488);
		border-color: transparent;
		color: white;
		box-shadow: 0 30px 60px rgba(13, 148, 136, 0.35);
	}

	.plan-badge {
		display: inline-flex;
		align-items: center;
		gap: 0.5rem;
		background: rgba(255, 255, 255, 0.15);
		color: white;
		border-radius: 999px;
		padding: 0.35rem 0.9rem;
		font-size: 0.85rem;
		font-weight: 600;
		letter-spacing: 0.04em;
		text-transform: uppercase;
	}

	.plan-name {
		font-size: 1.75rem;
		font-weight: 700;
		color: #0f172a;
	}

	.pricing-card.featured .plan-name {
		color: white;
	}

	.plan-description {
		color: #4b5563;
		line-height: 1.7;
		font-size: 1rem;
	}

	.pricing-card.featured .plan-description {
		color: rgba(255, 255, 255, 0.85);
	}

	.plan-price {
		display: flex;
		align-items: baseline;
		gap: 0.5rem;
	}

	.price-amount {
		font-size: 2.5rem;
		font-weight: 700;
		color: #0f172a;
	}

	.pricing-card.featured .price-amount {
		color: white;
	}

	.price-period {
		font-size: 1rem;
		color: #6b7280;
		font-weight: 500;
	}

	.pricing-card.featured .price-period {
		color: rgba(255, 255, 255, 0.75);
	}

	.plan-features {
		list-style: none;
		margin: 0;
		padding: 0;
		display: flex;
		flex-direction: column;
		gap: 0.75rem;
	}

	.plan-features li {
		position: relative;
		padding-left: 1.5rem;
		color: #4b5563;
		line-height: 1.6;
		font-size: 0.95rem;
	}

	.pricing-card.featured .plan-features li {
		color: rgba(255, 255, 255, 0.85);
	}

	.plan-features li::before {
		content: "+";
		position: absolute;
		left: 0;
		top: 0;
		color: #0d9488;
		font-weight: 700;
	}

	.pricing-card.featured .plan-features li::before {
		color: white;
	}

	.plan-button {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		padding: 0.9rem 1.5rem;
		border-radius: 999px;
		background: #0d9488;
		color: white;
		text-decoration: none;
		font-weight: 600;
		font-size: 1rem;
		margin-top: auto;
		transition:
			transform 0.3s ease,
			box-shadow 0.3s ease,
			background 0.3s ease;
	}

	.plan-button:hover {
		transform: translateY(-3px);
		box-shadow: 0 14px 30px rgba(13, 148, 136, 0.35);
	}

	.pricing-card.featured .plan-button {
		background: white;
		color: #0f766e;
	}

	.pricing-card.featured .plan-button:hover {
		background: #f4fdfb;
		color: #0b5c55;
		box-shadow: 0 14px 30px rgba(255, 255, 255, 0.25);
	}

	.cta-section {
		text-align: center;
	}

	.cta-button {
		background: #0d9488;
		color: white;
		padding: 1.2rem 2.5rem;
		border-radius: 50px;
		text-decoration: none;
		font-weight: 600;
		font-size: 1.1rem;
		transition: all 0.3s ease;
		display: inline-block;
		box-shadow: 0 4px 20px rgba(13, 148, 136, 0.3);
	}

	.cta-button:hover {
		background: #0f766e;
		transform: translateY(-2px);
		box-shadow: 0 8px 30px rgba(13, 148, 136, 0.4);
	}

	/* Mobile styles */
	@media (max-width: 768px) {
		.pricing {
			padding: 4rem 1rem;
		}

		.section-title {
			font-size: 2.5rem;
		}

		.section-subtitle {
			font-size: 1.1rem;
		}

		.pricing-grid {
			grid-template-columns: 1fr;
			gap: 2rem;
		}

		.pricing-card {
			padding: 2.25rem 1.75rem;
		}

		.plan-name {
			font-size: 1.5rem;
		}

		.plan-price {
			flex-direction: column;
			align-items: flex-start;
			gap: 0.25rem;
		}

		.plan-button {
			width: 100%;
		}

		.cta-button {
			padding: 1rem 2rem;
			font-size: 1rem;
		}
	}

	@media (max-width: 480px) {
		.section-title {
			font-size: 2rem;
		}

		.price-amount {
			font-size: 2.1rem;
		}

		.plan-features li {
			font-size: 0.9rem;
		}
	}
</style>

---
---

<section id="pricing">
	<div>
		<h2>Contract</h2>
		<p>Inaugurate your department of Consultology</p>
	</div>

	<div class="pricing-grid">
		<fluent-card>
			<h3>Attending</h3>
			<p>Create consult notes, templates, and workflows.</p>
			<div class="price">
				<span class="amount">$250</span>
				<span class="period">/month</span>
			</div>
			<ul>
				<li>Creates pre-visit consult notes from referral package</li>
				<li>Concisely summarizes investigations how you like it</li>
				<li>Fully customizable templates</li>
			</ul>
			<fluent-button href="/demo">Start free trial</fluent-button>
		</fluent-card>

		<fluent-card>
			<h3>Resident</h3>
			<p>Prepping the patient for visit.</p>
			<div class="price">
				<span class="amount">$125</span>
				<span class="period">/month</span>
			</div>
			<ul>
				<li>Customizable patient questionnaire</li>
				<li>Virtual patient onboarding</li>
				<li>Prepare draft pre-visit HPI and assessment plan</li>
			</ul>
			<fluent-button href="/demo">Book a demo</fluent-button>
		</fluent-card>

		<fluent-card>
			<h3>Student</h3>
			<p>A helping hand during your visit.</p>
			<div class="price">
				<span class="amount">$75</span>
				<span class="period">/month</span>
			</div>
			<ul>
				<li>Updates HPI during visit</li>
				<li>Summarizes patients' key concerns</li>
				<li>Finalizes assessment plan based on your recommendations</li>
			</ul>
			<fluent-button href="/contact">Talk to sales</fluent-button>
		</fluent-card>
	</div>

	<fluent-button appearance="accent" href="/demo">
		Try it free - save 4+ hours every week
	</fluent-button>
</section>

<style>
	section {
		padding: var(--spacingVerticalXXXL) var(--spacingHorizontalL);
		background: var(--colorNeutralBackground2);
		text-align: center;
	}

	h2 {
		color: var(--colorNeutralForeground1);
		margin-bottom: var(--spacingVerticalM);
	}

	section > div > p {
		color: var(--colorNeutralForeground2);
		margin-bottom: var(--spacingVerticalXXL);
	}

	.pricing-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
		gap: var(--spacingHorizontalXL);
		margin-bottom: var(--spacingVerticalXXL);
	}

	fluent-card {
		padding: var(--spacingVerticalXL) var(--spacingHorizontalL);
		display: flex;
		flex-direction: column;
		gap: var(--spacingVerticalL);
	}

	h3 {
		color: var(--colorNeutralForeground1);
	}

	fluent-card > p {
		color: var(--colorNeutralForeground2);
	}

	.price {
		display: flex;
		align-items: baseline;
		justify-content: center;
		gap: var(--spacingHorizontalS);
	}

	.amount {
		color: var(--colorNeutralForeground1);
	}

	.period {
		color: var(--colorNeutralForeground2);
	}

	ul {
		list-style: none;
		padding: 0;
		margin: 0;
		display: flex;
		flex-direction: column;
		gap: var(--spacingVerticalS);
		text-align: left;
	}

	li {
		color: var(--colorNeutralForeground2);
		padding-left: var(--spacingHorizontalL);
		position: relative;
	}

	li::before {
		content: "✓";
		position: absolute;
		left: 0;
		color: var(--colorBrandForeground1);
	}

	fluent-card fluent-button {
		margin-top: auto;
	}
</style>

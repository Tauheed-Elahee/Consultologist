---
---

<section id="home">
	<fluent-card>
		<h1>Consultologist: A physician specializing in consults</h1>
		<p>You handle <em>your</em> speciality, we handle <em>ours</em></p>
		<fluent-button appearance="accent" href="/demo">
			Try Consultologist Free - Save 4+ Hours/Week
		</fluent-button>
	</fluent-card>
</section>

<style>
	section {
		min-height: 100vh;
		display: flex;
		align-items: center;
		justify-content: center;
		padding: calc(var(--spacingVerticalXXXL) * 2) var(--spacingHorizontalL);
		padding-top: calc(80px + var(--spacingVerticalXXXL) * 2);
		background: var(--colorBrandBackground);
	}

	fluent-card {
		text-align: center;
		padding: var(--spacingVerticalXXL) var(--spacingHorizontalXL);
		max-width: 1200px;
	}

	h1 {
		color: var(--colorNeutralForeground1);
		margin-bottom: var(--spacingVerticalXL);
	}

	p {
		color: var(--colorNeutralForeground2);
		margin-bottom: var(--spacingVerticalXXL);
	}

	em {
		font-style: italic;
		font-weight: 600;
	}
</style>
